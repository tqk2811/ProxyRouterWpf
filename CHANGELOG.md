# Changelog

All notable changes to this project are documented here.
This file is generated by [git-cliff](https://git-cliff.org) from Conventional Commits.

A **timeline read bottom-to-top**: each `## [M.m.0]` is the tag that opened a minor line; the
commits stacked ABOVE it are that line's successive versions `M.m.1`, `M.m.2`, … (patch number =
commits since the tag), newest on top. Each line shows its version, conventional type, and message.

- 1.0.5 *(feat)* **(filters)** Enter a TotalBytes threshold as a value plus a unit
- 1.0.4 *(feat)* **(ui)** Add a byte threshold helper and decimal numeric input
- 1.0.3 *(docs)* Add a Vietnamese README and cross-link both
- 1.0.2 *(feat)* **(logs)** Show the tunnel start time as its own column
- 1.0.1 *(chore)* Stop tracking the Vietnamese glossary doc
## [1.0.0]

- *(docs)* Document the versioning and release flow [release]
- *(ci)* Add the master-only opt-in release pipeline
- *(build)* **(version)** Derive the app version from GitVersion tags
- *(docs)* Add demo screenshots to the README
- *(docs)* Explain tunnel, live row, FIFO and INotifyPropertyChanged
- *(feat)* Show tunnels in the logs while they are still open
- *(feat)* Allow multi-select in the filters grid
- *(docs)* Add a Vietnamese glossary
- *(fix)* Keep the bandwidth chart live after switching tabs
- *(chore)* Bump TqkLibrary.Proxy to 1.0.39
- *(feat)* Show direct (machine IP) label for logs without a picked source
- *(fix)* Make group/source drag-drop reorder match the insertion line
- *(feat)* Top-align content and watermark for multiline textboxes
- *(feat)* Shorten the bandwidth chart window to 60 seconds
- *(feat)* Draw value axes on the bandwidth chart
- *(feat)* Make datagrid columns resizable with visible dividers
- *(feat)* Restrict the log capacity box to digits
- *(feat)* Filter logs by date and time in one input
- *(feat)* Render TextBox placeholder from Tag
- *(fix)* Translate Filters tab header to Vietnamese
- *(feat)* Lock only the host list while running, widen proxy drag targets
- *(feat)* Warn when a matched routing group holds no proxy
- *(feat)* Split proxy sources into Hosts and Unassigned lists
- *(feat)* Drag-and-drop reorder and group assignment on Proxies grids
- *(fix)* Match textbox/passwordbox height with combobox and prevent text clipping
- *(refactor)* Overlay group heading on tab strip right side
- *(feat)* Add application icon for exe/taskbar
- *(refactor)* Streamline proxies server card and output panel layout
- *(fix)* Scope TabItem hand cursor to header so tab content uses default cursors
- *(refactor)* Remove Settings tab, move log FIFO capacity into Logs tab
- *(feat)* Add English/Vietnamese localization with runtime switching
- *(feat)* Title-bar tab strip, configurable listen address, context-menu row actions
- *(style)* Theme DatePicker to match dark/light palette
- *(fix)* Auto-refresh tunnel logs by default and add empty-state hint
- *(refactor)* Remove display name from settings
- *(feat)* List all bound IPv4 in a combobox for proxy output copy
- *(feat)* Resolve LAN/VPS IPv4 locally instead of calling ipify
- *(style)* Remove WPF badge from title bar
- *(docs)* Add README
- *(feat)* WPF UI (viewmodels, views, dialogs) with no proxy auto-start
- *(feat)* Proxy engine, in-RAM FIFO tunnel logs and bandwidth sampler
- *(feat)* Domain models, JSON config store and single-user services
- *(chore)* WPF project scaffold, dark/light theme and shell
## [Unreleased]

<!-- generated by git-cliff -->
