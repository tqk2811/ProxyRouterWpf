namespace ProxyRouterWpf.Localization
{
    /// <summary>UI language selection. <see cref="System"/> follows the OS UI culture (vi -> Vietnamese, else English).</summary>
    public enum AppLanguage
    {
        System = 0,
        English = 1,
        Vietnamese = 2,
    }
}
