<UserControl x:Class="ProxyRouterWpf.Views.BandwidthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ProxyRouterWpf.ViewModels"
             d:DataContext="{d:DesignInstance vm:BandwidthViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Download" Style="{StaticResource Subtle}"/>
                    <TextBlock Text="{Binding DownloadText}" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource Brush.Success}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Upload" Style="{StaticResource Subtle}"/>
                    <TextBlock Text="{Binding UploadText}" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource Brush.Info}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusText}" Style="{StaticResource Subtle}" Margin="0,8,0,0"/>
                <Border ClipToBounds="True">
                    <Canvas x:Name="ChartCanvas" Background="Transparent" SizeChanged="Chart_SizeChanged"/>
                </Border>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
