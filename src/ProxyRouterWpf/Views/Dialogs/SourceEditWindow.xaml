<Window x:Class="ProxyRouterWpf.Views.Dialogs.SourceEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proxy" Width="460" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Surface.Background}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        TextOptions.TextFormattingMode="Display">
    <Border BorderBrush="{DynamicResource Brush.Surface.BorderBrush}" BorderThickness="1">
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="{DynamicResource Brush.TitleBar.Background}" Padding="16,12" MouseLeftButtonDown="Header_Drag">
                <TextBlock Text="Sửa proxy" FontWeight="SemiBold" FontSize="14"/>
            </Border>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
                <Button Content="Huỷ" Click="Cancel_Click" MinWidth="84"/>
                <Button Content="Lưu" Style="{StaticResource AccentButton}" Click="Ok_Click" MinWidth="84" Margin="8,0,0,0"/>
            </StackPanel>
            <StackPanel Margin="16,14">
                <Label Content="Nhóm"/>
                <ComboBox x:Name="GroupBox" DisplayMemberPath="Label"/>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="Loại"/>
                        <ComboBox x:Name="TypeBox"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <Label Content="Port"/>
                        <TextBox x:Name="PortBox"/>
                    </StackPanel>
                </Grid>
                <Label Content="Address" Margin="0,10,0,0"/>
                <TextBox x:Name="AddressBox"/>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="Username"/>
                        <TextBox x:Name="UserBox"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <Label Content="Password"/>
                        <TextBox x:Name="PassBox"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </DockPanel>
    </Border>
</Window>
